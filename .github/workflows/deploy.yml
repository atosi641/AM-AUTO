name: Auto Deploy One.com
on:
  push:
    branches:
    - main
  workflow_dispatch:
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Test Deploy
      run: echo "AUTO-SYNC WORKS $(date)"
