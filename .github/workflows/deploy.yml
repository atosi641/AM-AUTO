name: Deploy to One.com (FIX)
on:
  push:
    branches: [ main ]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Install lftp
      run: sudo apt-get update && sudo apt-get install -y lftp
      
    - name: Deploy ALL Files
      run: |
        lftp -e "
        set sftp:auto-confirm yes;
        open -u ${{ secrets.SFTP_USER }},${{ secrets.SFTP_PASS }} sftp://ssh.cyqbonh8m.service.one;
        cd '/customers/9/8/9/cyqbonh8m/webroots/281524e/am-auto.se/';
        mirror -R -v --delete --verbose .;
        bye
        " -c
